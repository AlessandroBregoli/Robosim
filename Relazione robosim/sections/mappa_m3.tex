\renewcommand{\mapComplexity}{1.29}
\renewcommand{\nomeMappa}{Mappa 3}
\renewcommand{\mapDescription}{La mappa 3 risulta avere un intralcio medio superiore
 alle precendenti mappe. In questo caso si ha un'ulteriore complessità data dalla
 stanza interna che presenta un solo ingresso di piccole dimensioni in modo da 
 mettere in difficoltà gli algoritmi greedy. Possiamo vedere come i tempi medi di esecuzione
 siano comparabili; tuttavia l'algoritmo $A^*$ risulta significativamente più
 stabile. È inoltre da notare il fatto che l'algoritmo simple può, in rari casi,
 entrare in loop e dunque non terminare; questi casi non sono presi in considerazione ai fini
 delle statistiche.}
\renewcommand{\pSimpleFit}{0.87}
\renewcommand{\pAstarFit}{0.87}
\renewcommand{\errSimpleFit}{14.49}
\renewcommand{\errAstarFit}{7.46}

